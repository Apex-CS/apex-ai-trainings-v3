server:
  port: 8080

spring:

  ai:

    ollama:

      base-url: http://localhost:11434

      chat:
        model: llama3.2

      embedding:
        model: nomic-embed-text

logging:
  level:
    org.springframework.web.client: TRACE
    org.springframework.http.client: TRACE
    org.springframework.ai.chroma: TRACE